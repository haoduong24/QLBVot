@model IEnumerable<QLBVot.Models.HoaDon>

@{
    ViewBag.Title = "Index";
}

<h2>Hoá đơn</h2>

<p>
    @Html.ActionLink("Thêm mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NgayLapHoaDon)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TongTien)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DonHang.TenKhachHang)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NgayLapHoaDon)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TongTien)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DonHang.TenKhachHang)
        </td>
        <td>
            @Html.ActionLink("Sửa", "Edit", new { id=item.MaHoaDon }) |
            @Html.ActionLink("Thông tin", "Details", new { id=item.MaHoaDon }) |
            @Html.ActionLink("Xoá", "Delete", new { id=item.MaHoaDon })
        </td>
    </tr>
}

</table>
